mongodb:
  image: mongo:3.0
  command: mongod --smallfiles --quiet
nginx:
  build: ./config/nginx
  ports:
    - "80:80"
railsapp:
  build: .
  command: bundle exec rails s -p 3000 -b '0.0.0.0'
  volumes:
    - .:/myapp
